# 测试方案：对局数据维护能力

---

## 一、整体表格维护能力

### 1.1 数据导入
| 编号 | 测试项 | 输入 | 期望结果 |
|------|--------|------|----------|
| I-1 | 全量 TOML 导入 | `matches_copytest.toml` (26条) | 26 条入库，无遗漏 |
| I-2 | 增量导入 | 新增 3 条的 TOML | 仅追加 3 条，原有不变 |
| I-3 | 重复 ID 导入 | 含已有 ID 的 TOML | 跳过或报错，不覆盖 |
| I-4 | 空文件导入 | 空 TOML | 无变化，不报错 |

### 1.2 数据导出
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| E-1 | 导出 TOML | `mm export --format toml` | 输出与原始 TOML 结构一致 |
| E-2 | 导出 JSON | `mm export --format json` | 合法 JSON，26 条记录 |
| E-3 | 导出 CSV | `mm export --format csv` | 表格格式，含表头 |
| E-4 | 导出后重新导入 | 导出→删除→导入 | 数据完全恢复 |

### 1.3 全量验证
| 编号 | 测试项 | 场景 | 期望结果 |
|------|--------|------|----------|
| V-1 | 正常数据 | 26 条完整对局 | ✅ 全部通过 |
| V-2 | 角色名错误 | 改一个角色名为不存在 | ❌ 报具体行号+错误名 |
| V-3 | 缺失必填字段 | 删除某条的 `result` | ❌ 报缺失字段 |
| V-4 | 重复角色 | 同一队出现两次"饼干" | ❌ 报重复 |
| V-5 | result 非法值 | 改为 `"draw"` | ❌ 仅接受 `attacker_win`/`defender_win` |

### 1.4 批量操作
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| B-1 | 批量删除 | 删除 ID 0001-0005 | 剩 21 条 |
| B-2 | 批量修改字段 | 所有 `source="论坛"` → `trust="medium"` | 14 条更新 |
| B-3 | 批量清空 | 清空全部 | 0 条，数据库文件保留 |

### 1.5 备份与归档
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| A-1 | 自动备份 | 修改前自动备份 | 生成 `matches_backup_时间戳.toml` |
| A-2 | 归档命令 | `mm archive` | 当前数据移入 archive/，重置为空 |

---

## 二、单独记录编辑能力

### 2.1 新增记录
| 编号 | 测试项 | 输入 | 期望结果 |
|------|--------|------|----------|
| C-1 | 完整新增 | 所有字段齐全 | 入库成功，返回 ID |
| C-2 | ID 自增 | 不指定 ID | 自动分配下一个序号 |
| C-3 | 可选字段省略 | 省略 `notes`、`custom_def_tag` | 使用默认空值 |

### 2.2 修改记录
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| U-1 | 修改 result | `0001` → `defender_win` | 仅改该字段 |
| U-2 | 修改阵容 | 替换 `defender_team` | 5 人完整替换 |
| U-3 | 修改爆裂链 | 更新 `attacker_burst` | 多行文本正确写入 |
| U-4 | 修改多个字段 | 同时改 `result`+`margin`+`notes` | 全部更新 |

### 2.3 删除记录
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| D-1 | 删除单条 | `mm delete 0001` | 剩 25 条 |
| D-2 | 删除不存在 | `mm delete 9999` | 报错提示不存在 |

### 2.4 查询记录
| 编号 | 测试项 | 操作 | 期望结果 |
|------|--------|------|----------|
| Q-1 | 查单条 | `mm show 0001` | 显示完整字段 |
| Q-2 | 查不存在 | `mm show 9999` | 报错提示 |
| Q-3 | 按角色搜索 | 搜含"诺雅"的对局 | 返回所有含诺雅的对局 |

---

## 三、边界与异常

| 编号 | 测试项 | 场景 | 期望结果 |
|------|--------|------|----------|
| X-1 | 并发写入 | 两个进程同时修改 | 不丢失数据，锁机制生效 |
| X-2 | 超长 notes | 500 字备注 | 正常存储 |
| X-3 | 特殊字符 | 角色名含 `: `、`（）` | 正确转义存储 |
| X-4 | 日期格式错误 | `date = "2026/05/08"` | ❌ 仅接受 `YYYY-MM-DD` |
| X-5 | 数据库文件损坏 | 手动破坏 `.db` 文件 | 报错+提示修复 |
| X-6 | 磁盘满写入 | 模拟写入失败 | 回滚，不产生脏数据 |

---

## 四、测试执行顺序

```
Phase 1: 基础 CRUD（C-1 → Q-1 → U-1 → D-1）
Phase 2: 导入导出（I-1 → E-1 → E-2 → E-3 → E-4）
Phase 3: 批量操作（B-1 → B-2 → B-3）
Phase 4: 验证系统（V-1 → V-2 → V-3 → V-4 → V-5）
Phase 5: 边界异常（X-1 → X-6）
Phase 6: 备份归档（A-1 → A-2）
```

共 **30 个测试用例**，覆盖增删改查、导入导出、批量操作、验证、边界。
